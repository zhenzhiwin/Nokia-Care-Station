#TextFSM template for MME AHP
#
Value host (HZMME\d+BNK)
Value unit (\w+-\d+)
#Value equiptype (\w+)
Value level (\*+)
Value date (\d+-\d+-\d+)
Value time (\d+:\d+:\d+\.\d+)
Value alarmid (\d+)
Value alarmtext ([\w\s]+)

Start
  ^ALARM HISTORY
  ^\s+<HIST>\s+${host}\s+${unit}\s+\w+\s+${date}\s+${time}
  ^${level}\s+\w+
  ^\s+\(\d+\)\s+${alarmid}\s+${alarmtext} -> Record

#End of template