#TextFSM template for MME AHO
#
Value host (\w+)
Value unit (\w+-\d+)
#Value equiptype (\w+)
Value level (\*+)
Value date (\d+-\d+-\d+)
Value time (\d+:\d+:\d+\.\d+)
Value alarmid (\d+)
Value alarmtext ([\w\s]+)
Value hexinfo ([\d\w\s]+)

Start
  ^ALARMS CURRENTLY ON
  ^\s+${host}\s+${unit}\s+\w+\s+${date}\s+${time}
  ^${level}\s+\w+
  ^\s+\(\d+\)\s+${alarmid}\s+${alarmtext}
  ^\s+${hexinfo} -> Record

#End of template